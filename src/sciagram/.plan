2026-09-19
I am now trying to write a converter class for sciagram. I want this to work in the way that the end user writes from sciagram.converter import Converter. Hmm, for that I will have to edit the __init__.py as well. But let me think about the structure for the class itself - what configuration options should I present here?
Let me treat the class as a blackbox for now,
input (image URL) -> ??? -> output (a 2D array of ASCII chars). I should probably also provide a printing options for this later, maybe using pretty_print. But for now, I am only concerned with config options. Three things come to mind:
1. brightness measurement method (three literalts, luminosity minmax or average)
2. ASCII character sequence itself.
3. the option to set true_term to True or False.
I think that's it for now.

Done. It works now.
Interestingly an LLM introduced me to shutil.get_terminal_size which allows me to set a fallback terminal size (defaulting to (80, 24)). This is useful because when I tried to uv run | cat before i was hitting unrecognised ioctl device, and now I can cat it easily.

I know that the proportions thing is also a priority, but I am more interested in colors right now.

Now let me tackle RGB color generation mode. Should be easy with Pillow. I already have the RGB tuples and I just need to wrap each printed character with ANSI escape codes. What I can do
 right now is to check how pillow does this - just import PIL and test an image and check how to get the color data from it. When I have the RGB data I will write a simple
 function that checks which of the three values is the most prevalent, and on basis of that assigns an R or G or B color. But wait, this is not true color representation.
 What if I want TrueColor 24-bit ANSI? I think I will have to map specific RGB values to ANSI codes.
 Let me discuss this with the brainstorm or pacer skill.
